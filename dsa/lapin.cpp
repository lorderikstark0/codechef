#include <iostream>
#include <string>
#include <algorithm>

using namespace std;
//not solveed

int main()
{
    int n ;
    cin>>n;
    while(n-->0)
    {
        string p;
        cin >>p;
        //since we have to count the frequency of the eac characters after divide
        //from the middle 
        int y =strlen(p);
        string t;
        string h;
        if(y/2==0){
        t=p.substr(0,y/2)
        h=p.substring(y/2,y-1);
    }
        else {

        }
   }
}