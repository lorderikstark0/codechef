#include <bits/stdc++.h>

#define pb push_back 
#define ll long long 
#define forit(it,a) for(_typeof(a.begin()) it =a.begin();it!=a.end();it++)
#define mp make_apir 

using namespace std;


int main(){
	bool status=true;
	while(status){
		int t;
		cin >> t;
		if(t==42){
			status=false;
			break;
		}
		cout << t << "\n";
	}
	return 0;
}